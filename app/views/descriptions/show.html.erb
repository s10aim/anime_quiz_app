<div class="container mt-3">
  <% if @package.category == "complete" %>
    <h1 class="desc-title" data-en="ANIAQ">総合問題</h1>
    <%= simple_form_for @package, url: descriptions_path, method: :post do |f| %>
      <p><%= f.input :category, as: :hidden, input_html: { value: params[:category] } %></p>
      <div class="desc-content-box">
        <span class="box-title">ルール説明</span>
        <p>・総合問題では投稿された全クイズの中からランダムで問題が出題されます。</p>
        <p>・問題は全10問です。</p>
        <p>・スタートボタンを押すと問題がスタートします。</p>
        <p>・最後のクイズに回答すると終了です。</p>
        <p>・スタートしてから最後のクイズに回答するまでの所要時間を計測します。</p>
        <p>・獲得したスコアから所要時間の秒数を引いたスコアをもとにランキングを計測します。</p>
        <p>・よって、所要時間が短いほどスコア計算時に得点が高くなります。</p>
        <p>・ランキングは総合問題に回答した人のみで計測します。</p>
        <p>・スコアが同点の場合は先着順になります。</p>
        <p class="mt-4">1位を目指して頑張ってください✨</p>
      </div>
      <div class="desc-btn-content">
        <div class="desc-start">
          <%= f.button :submit, "スタート", class: "btn-start" %>
        </div>
        <div class="desc-back">
          <%= link_to home_path do %>
            <button class="btn-back">
              <%= image_tag 'back_arrow.svg' %>
              <span>Home</span>
            </button>
          <% end %>
        </div>
      </div>
    <% end %>
  <% end %>
  <% if @package.category == "selected" %>
    <h1 class="my-5">アニメを選択してクイズに挑戦</h1>
    <div class="mb-3">
      <%= simple_form_for @package, url: descriptions_path, method: :post do |f| %>
        <p>
          <%= f.input :anime_id, as: :select,
                                 collection: @select_animes_collection,
                                 prompt: '選択してください',
                                 label: "#{Anime.model_name.human}作品" %>
        </p>
        <p><%= f.input :category, as: :hidden, input_html: { value: "selected" } %></p>
        <div class="card mb-5" style="height: 26rem;">
          <div class="card-body">
            クイズの説明
          </div>
        </div>
        <%= f.button :submit, "スタート", class: "btn btn-primary" %>
      <% end %>
    </div>
  <% end %>
</div>
